name: Build and deploy
on:
  pull_request:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - uses: jakejarvis/s3-sync-action@master
      with:
        args: --acl public-read --follow-symlinks --delete --exclude '.git*/*'
      env:
        AWS_S3_BUCKET: optit
        AWS_ACCESS_KEY_ID: AKIA2BNAHQ7VXZEKS43N
        AWS_SECRET_ACCESS_KEY: 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
